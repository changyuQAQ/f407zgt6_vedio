基于 STM32F4 和 FreeRTOS 的本地视频播放器，核心流程可以简单概括为以下三点：
1. 任务分配
按键任务（优先级最高）：只负责扫键，按下后设置标志位，不阻塞其他任务。
读卡任务（生产者）：从 SD 卡读数据。读到视频块就塞进双缓冲队列；读到帧尾的音频数据就喂给音频模块。
显示任务（消费者）：按帧率定时从队列取视频块，通过 DMA 刷到屏幕上。
2. 视频播放
采用双缓冲 + 环形队列解耦读写速度：
读卡任务往 block_buf[0/1] 里填数据，更新写指针 q_head。
显示任务等帧率时间到了，就从当前缓冲区起 LCD DMA 传输，更新读指针 q_tail。
队列满时读卡任务主动让出 CPU，队列空时显示任务延时等待，避免画面撕裂。
3. 音频怎么响
防止 SD 卡读取抖动导致爆音，采用三级缓冲：
软件 FIFO（CCM RAM）：读卡任务在临界区内把音频帧写入这里。
DMA 双缓冲（SRAM1）：DMA 传完半区触发中断，ISR 直接从 FIFO 搬数据填满另一半。如果 FIFO 没数据，就用上一个采样值填充（Hold），绝不补零，避免“啪”的爆音。
预填充机制：切歌或刚启动时，先静默填满 DMA 缓冲区和一部分 FIFO，达到阈值再开 DMA，保证开口第一声就是稳的
